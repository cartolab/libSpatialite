<project name="extSqliteUsers" default="generate-without-source" basedir=".">
    <description>
    	Users management and DB Connection extension
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src/es/udc/cartolab/gvsig/spatiaLiteConnector"/>
  <property name="build" location="bin"/>
  <property name="dist"  location="dist"/>
  <property name="extensionsDir" location="../_fwAndami/gvSIG/extensiones"/>
  <property name="mainplugin" value="com.iver.cit.gvsig"/>
  <property name="spatialitejar" value="sqlite_spatial"/>
  <property name="sqlitejar" value="sqlite-jdbc-3.7.15"/>
  <property name="plugin" value="es.udc.cartolab.gvsig.sqliteUsers"/>
  <property name="andami" location="../_fwAndami" />
  <property name="drivers-dir" location="${extensionsDir}/${mainplugin}/drivers/jdbc_spatial" />
  <property name="lib-dir" location="${extensionsDir}/${mainplugin}/lib"/>
  <property name="lib" location="lib"/>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
    <mkdir dir="${dist}"/>

  </target>

  <target name="generate-without-source"
  		description="generate the distribution without the source file">
    <!-- Create the distribution directory -->
    <mkdir dir="${dist}"/>
  	<mkdir dir="${dist}/lib"/>
  	  	
    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
	<copy todir="${dist}/lib">
  	    	<fileset dir="lib" includes="*"/>
  	</copy>

  	<jar jarfile="${dist}/${spatialitejar}.jar" basedir="${build}"
  		includes="es/udc/cartolab/cit/gvsig/fmap/drivers/jdbc/spatialite/**"
  	/>

  	<copy todir="${drivers-dir}"  overwrite="true">
  		<fileset dir="${dist}" includes="${spatialitejar}.jar" />
 	</copy>

    <copy todir="${lib-dir}/"  overwrite="true">
  		<fileset dir="${dist}" includes="${spatialitejar}.jar"/>
  	    <fileset dir="${lib}" includes="${sqlitejar}.jar"/>
    </copy>
  </target>


	<target name="compile" description="compile the source" >
		<!-- Compile the Java code from ${src} to ${build} -->
		<mkdir dir="${build}" />
		<echo>${compile-classpath}</echo>
		<javac  srcdir="${src}"
			destdir="${build}"
			debug="${debug}"
			debuglevel="${debuglevel}"
			classpath="${compile-classpath}"/>
	</target>

	<target name="copy-driver-to-andami">
	  	<copy todir="${drivers-dir}"  overwrite="true">
	  		<fileset dir="${dist}" includes="${spatialitejar}.jar" />
	 	</copy>
	
	    <copy todir="${lib-dir}/"  overwrite="true">
	  		<fileset dir="${dist}" includes="${spatialitejar}.jar"/>
	  	    <fileset dir="${lib}" includes="${sqlitejar}.jar"/>
	    </copy>
	</target>


	<target name="clean"
	        description="clean up" >
	    <!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${dist}"/>
		<delete dir="${build}"/>
	</target>
</project>

